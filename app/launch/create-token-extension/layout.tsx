import { Metadata } from "next";
import React from "react";
export const metadata: Metadata = {
  title: "No-Code Solana Token Extension Creator | Guacamole",
};
const layout = ({ children }: { children: any }) => {
  return <>{children}</>;
};

export default layout;
